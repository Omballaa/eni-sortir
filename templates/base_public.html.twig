<!DOCTYPE html>
<html{% if app.environment == 'dev' %} data-env-dev{% endif %}>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ENI Sortir{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        {# Bootstrap CSS via CDN - Version 5.3 #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        
        {# Icônes Bootstrap #}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
        
        {% block stylesheets %}
            {# Aucun CSS personnalisé - uniquement Bootstrap #}
        {% endblock %}

        {% block javascripts %}
            {# Bootstrap JS via CDN #}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
        {% endblock %}
    </head>
    <body class="bg-light">
        {# Header simple pour les pages publiques #}
        <header class="bg-primary text-white text-center py-3 mb-4">
            <div class="container">
                <h1 class="h3 mb-0">
                    <i class="bi bi-calendar-event me-2"></i>
                    ENI Sortir
                </h1>
                <p class="mb-0 small">Application de gestion des sorties</p>
            </div>
        </header>

        {# Contenu principal #}
        <main>
            {% block body %}{% endblock %}
        </main>

        {# Footer simple #}
        <footer class="bg-light mt-5 py-3 border-top">
            <div class="container">
                <div class="text-center text-muted">
                    <small>&copy; {{ "now"|date("Y") }} ENI Sortir - École Nationale d'Informatique</small>
                </div>
            </div>
        </footer>
    </body>
</html>